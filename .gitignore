# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Vercel
.vercel/

# Backups de produção — contêm dado real (PII de usuários, linhas de tabela completas).
# Gerados sob demanda antes de operações destrutivas em produção; nunca versionar.
backup_pre_migrations_*/
*.bundle

# Lixo temporario local (lock files de git, pastas de saida do Claude) - nunca versionar
_to_delete/
Claude outputs/
